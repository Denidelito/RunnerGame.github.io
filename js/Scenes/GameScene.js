import { SCT} from "../SCT.js";

export class GameScene extends Phaser.Scene {
    constructor() {
        super({
            key: SCT.SCENES.GAME
        });
    }
    init() {

    }
    create() {
    }
    update(time, delta) {
    }
}